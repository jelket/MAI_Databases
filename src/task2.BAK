
select gai.name from gai where gai.id not in ;
(select dtp.gai_id from dtp;
	join participant_of_dtp pod on pod.dtp_id = dtp.id ;
	join vehicle on pod.vehicle_id = vehicle.id;
where vehicle.pts = 111)
